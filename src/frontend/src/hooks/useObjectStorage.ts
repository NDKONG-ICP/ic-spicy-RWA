/**
 * Image compression utility.
 * Pure canvas-based compression with no external storage dependencies.
 * Re-exported here for backward compatibility; canonical source is src/utils/imageUtils.ts.
 */
export { compressImage } from "../utils/imageUtils";
